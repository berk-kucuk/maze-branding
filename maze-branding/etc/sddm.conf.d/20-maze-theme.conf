# Maze Linux login/greeter theme.
#
# Uses KDE's rock-solid Breeze SDDM theme (Qt6, always renders) with the Maze
# wallpaper behind it — see /usr/share/sddm/themes/breeze/theme.conf.user. The
# custom 'maze-oled' theme is kept in the tree but NOT the default (it had a
# render issue on first boot; Breeze is the reliable choice).
[Theme]
Current=breeze
